user = ["rr","ei","eyu"]
password = ("20920","h2uhi","2jdfp")

users = list(zip(user,password))

#for i in users:
    #print(i)

print(list(users))  c
#print(dict(users))
#zip 物件是一次性的。
#Python 的做法（正確）： 拿一個清單，把裡面的東西數一遍，再放進一個新的清單 $\rightarrow$ [...]